import React from 'react';

import '../../styles/components/Header.css';

const Header = () => (
  <header className="header">
    <h1 className="header__title">BGC Inventory Scanner<span role="img" aria-label="shopping cart">🛒</span></h1>
  </header>
);

export default Header;